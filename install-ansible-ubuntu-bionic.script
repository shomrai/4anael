# Run by e.g.: sudo sh install-ansible...
apt update
apt install --yes software-properties-common
apt repository --yes -u ppa:ansible/ansible
apt --yes install ansible
ansible --version

apt install python-argcomplete
# sudo activate-global-python-argcomplete IS NOT WORKING! use the below instead:
python3 -m pip install argcomplete

